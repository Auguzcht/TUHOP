/**
 * Auth hook — thin wrapper around AuthContext.
 *
 * For most components, import { useAuthContext } from "@/contexts/auth-context"
 * directly. This file re-exports for backward compatibility.
 */
export { useAuthContext as useAuth } from "@/contexts/auth-context";
